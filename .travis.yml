language: python
python:
- 3.5

# Assume bundler is being used, therefore
# the `install` step will run `bundle install` by default.
script: ./pan_indent_checker.py Example-Before.pan Travis-Test.pan && diff Example-After.pan Travis-Test.pan


cache: bundler

sudo: false
